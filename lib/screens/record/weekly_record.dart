import 'package:flutter/material.dart';

class WeeklyRecordView extends StatelessWidget {
  const WeeklyRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Weekly Records'),
    );
  }
}
