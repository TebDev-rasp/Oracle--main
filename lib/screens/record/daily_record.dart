import 'package:flutter/material.dart';

class DailyRecordView extends StatelessWidget {
  const DailyRecordView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daily Records'),
    );
  }
}
