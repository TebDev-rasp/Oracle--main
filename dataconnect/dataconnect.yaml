specVersion: "v1beta"
serviceId: "oracle"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "oracle-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
